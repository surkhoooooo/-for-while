let arr = [14, 18, 12, 33, 11, 99, 73, 90, 101, 9,];
let = accumulatorOdd = [];

for (let i = 0; i <=9; i++) {
    if (arr[i] % 2 !== 0) {
        accumulatorOdd. push(arr[i]);
    }
}

console.log(accumulatorOdd);